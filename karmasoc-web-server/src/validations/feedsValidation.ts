'use strict';
/*const Joi = require('joi');
let validations :any = {};


validations.postByName = Joi.object().keys({
    userName: Joi.string()
});

validations.userIDPosts = Joi.object().keys({
    userId: Joi.string().required()
});
*/