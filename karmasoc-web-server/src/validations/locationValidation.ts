'use strict';
/*const Joi = require('joi');
let validations :any = {};

validations.coordinates = Joi.object().keys({
    long: Joi.number().default(40.7280004),
    lat: Joi.number().default(-74.0358875),
    postName: Joi.string().allow('')
});

validations.nearbyQuery = Joi.object().keys({
    long: Joi.number().default(40.7280004),
    lat: Joi.number().default(-74.0358875),
    maxDistance: Joi.number().default(2),
    limit: Joi.number().default(20)
});
validations.nearbyQueryOptional = Joi.object().keys({
    long: Joi.number().default(40.7280004),
    lat: Joi.number().default(-74.0358875),
    maxDistance: Joi.number().default(2),
    limit: Joi.number().default(20)
});

validations.postLocation = Joi.object().keys({
    long: Joi.number().default(40.7280004),
    lat: Joi.number().default(-74.0358875)
});*/