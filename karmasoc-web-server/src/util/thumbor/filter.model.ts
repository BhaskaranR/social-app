
export type FiltersState = {
    contrast: number,
    blur: number,
    grayScale: number,
    opacity: number,
    saturate:  number,
    blend: number,
    brightness: number,
    hueRotate: number,
    sepia: number,
    invert: number,
    mixBlendMode: string;
    background: string;
}